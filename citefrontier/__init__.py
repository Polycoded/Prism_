"""CiteFrontier - corpus retrieval and fusion reference components.

This package retains only the fixed Corpus Retrieval and Fusion component
(Module 02): the shared corpus record types (`models`), the text helpers that
retrieval depends on (`text`), and the retrievers (`retrieval`). All other
system modules are specified as blueprints under `docs/` and are not shipped
as code.
"""