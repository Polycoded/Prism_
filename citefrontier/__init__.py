"""CiteFrontier — reference components.

This package retains the fixed Corpus Retrieval and Fusion component
(Module 02: `models`, `text`, `retrieval`) and the shipped Retrieval
Controller reference (Module 04: `controller`). All other system modules are
specified as blueprints under `docs/` and are not shipped as code.
"""